# HIDE @ KYOWON — 에셋 스펙

이미지를 만들어서 **아래 경로에 같은 파일명·같은 픽셀 크기**로 넣으면 코드 수정 없이 바로 반영됩니다.
(현재는 `scripts/import-limezu.mjs` 가 LimeZu 무료팩에서 잘라 만든 플레이스홀더가 들어 있음)

- 위치: `public/assets/...`
- 형식: **PNG (투명 배경)**, 픽셀 아트, `image-rendering: pixelated`
- 좌표계: **탑다운 3/4 시점** (약간 위에서 비스듬히 내려다보는 각도. 가구는 정면 얼굴이 아래쪽을 향함)
- 타일 1칸 = **32 x 32 px**

---

## 1. 캐릭터 (가장 중요)

### 파일
`public/assets/characters/chars.png` — **스프라이트시트**

### 규격
- 프레임 크기: **32 x 64 px** (가로 32, 세로 64 — 캐릭터는 2칸 높이)
- 배치: **가로 6칸 × 세로 N줄** (줄 = 캐릭터 종류, 최대 6종 → 최종 192 x 384)
- 한 줄(캐릭터 1명)의 6프레임 순서:

| 프레임 | 내용 |
|---|---|
| 0 | 아래 보고 **정지** |
| 1 | 아래 보고 **걷기** (다리 벌린 자세) |
| 2 | 위(뒤통수) 보고 정지 |
| 3 | 위 보고 걷기 |
| 4 | **옆(왼쪽)** 보고 정지 |
| 5 | 옆(왼쪽) 보고 걷기 |

- 오른쪽은 코드가 좌우반전으로 처리 → 4·5번은 왼쪽만 그리면 됨
- 캐릭터는 프레임 **아래쪽 중앙(발끝)** 기준으로 정렬. 머리 위 여백 OK, 발이 잘리면 안 됨
- 배경 완전 투명

### 스타일
작고 귀여운 **치비 회사원** (머리 크고 몸 작게, 2~3등신). 셔츠+슬랙스+구두. 사원증 목걸이 있으면 굿.
6종은 스펙 문서 이미지의 `기본/사원/대리/과장/부장/인턴` 느낌으로 옷·머리색만 다르게.

### GPT / Gemini 로 만들 때 (AI 는 스프라이트시트를 정확히 못 그림)
1. **프레임 하나씩** 따로 뽑는 게 안정적:
   > "top-down 3/4 view chibi pixel-art office worker, facing camera, standing idle, full body, transparent background, 32x64 proportion, simple flat colors, thick outline"
2. 방향별로: `facing camera` / `seen from behind` / `seen from the left side`
3. 걷기 프레임: `same character, walking, one leg forward`
4. 크게 뽑아서 주시면(예: 512px) 제가 32x64 로 축소·정리해서 시트로 합칩니다.
5. **6프레임 다 없어도 됨** — 정지 4방향만 주시면 걷기는 제가 다리 흔들림으로 처리 가능

> 최소한: `아래·위·옆` 정지 3장 (캐릭터 1명) 이면 게임 돌아감. 6종 × 3장이면 완성형.

---

## 2. 타일 (바닥 / 벽 / 문) — 각 **32 x 32**, 상하좌우 이어붙여도 티 안 나게 (seamless)

`public/assets/tiles/`

| 파일 | 용도 | 느낌 |
|---|---|---|
| `floor-office.png` | 사무실 바닥 | 밝은 카펫/타일 (청회색 or 크림) |
| `floor-corridor.png` | 복도 | 밝은 리놀륨/장판 |
| `floor-meeting.png` | 회의실 | 우드 마루 |
| `floor-restroom.png` | 화장실 | 흰/민트 타일 (작은 격자) |
| `floor-pantry.png` | 탕비실 | 옅은 우드/베이지 |
| `wall.png` | 벽 (충돌) | 사무실 벽면 — 윗변에 약간 밝은 하이라이트, 아래 걸레받이 그림자 |
| `wall-low.png` | 화장실 칸막이 | 낮은 흰색 파티션 |
| `door.png` | 문 | 나무문 + 손잡이 (바닥 위에 얹힘, 밟고 지나감) |
| `elevator-door.png` | **64 x 64** 엘리베이터 문 | 금속 양문 + 층 표시등 + 호출버튼 |

> 벽은 픽셀 게임에서 보통 "윗면 + 정면" 2단으로 그리는데, 지금 구조는 1칸(32x32)이라
> **정면 벽면 한 장**으로 충분합니다. (원하면 나중에 윗면 캡 타일 `wall-top.png` 추가 가능)

---

## 3. 가구 / 소품 — 크기 제각각, **정면이 아래를 향하게**, 투명 배경

`public/assets/furniture/`

| 파일 | 권장 크기(px) | 설명 |
|---|---|---|
| `desk.png` | 56 x 50 | 사무 책상 (위에 모니터/서류) |
| `monitor.png` | 40 x 40 | 모니터 단품 (책상 위 데코) |
| `chair.png` | 26 x 42 | 사무용 의자 (옆모습, 오른쪽 향함) |
| `cabinet.png` | 64 x 56 | 나무 캐비닛 |
| `bookshelf.png` | 52 x 74 | 책장 |
| `printer.png` | 32 x 28 | 복합기 |
| `water-cooler.png` | 24 x 42 | 정수기 |
| `meeting-table.png` | 128 x 64 | 긴 회의 테이블 |
| `whiteboard.png` | 96 x 32 | 화이트보드 (벽걸이) |
| `bulletin.png` | 62 x 42 | 게시판 |
| `rug.png` | 96 x 56 | 러그 (충돌 없음) |
| `plant.png` | 46 x 62 | 화분 (야자/큰 잎) |
| `plant-bush.png` | 36 x 62 | 화분 (둥근 관목) |
| **`sink.png`** | 32 x 24 | **화장실 세면대** ← LimeZu 무료팩에 없음, 새로 필요 |
| **`toilet.png`** | 24 x 32 | **변기 (탑다운)** ← 새로 필요 |
| **`urinal.png`** | 20 x 28 | (선택) 소변기 |

크기 딱 안 맞아도 됨 — 제가 코드에서 조정합니다. **비율만 비슷하게.**

---

## 4. 배경 / 연출용

`public/assets/bg/`

| 파일 | 크기 | 설명 |
|---|---|---|
| `window-namsan.png` | 256 x 88 (가로로 늘여 씀) | 사무실 창문 프레임 + **서울 야경 / 남산타워** 실루엣. 창틀(십자 격자) 포함 |

`public/assets/ui/` (선택 — 없으면 지금 CSS 로 대체)

| 파일 | 크기 | 설명 |
|---|---|---|
| `logo.png` | ~480 x 160 | "HIDE @ KYOWON" 타이틀 로고 (픽셀 폰트 느낌) |
| `lobby-bg.png` | 1280 x 720 | 로비 배경 (어두운 사무실 건물 야경). 없으면 `window-namsan` 재활용 |

---

## 5. 우선순위

1. **캐릭터** `chars.png` (또는 방향별 낱장) — 이게 게임 인상의 절반
2. `window-namsan.png` — 로비·숨는시간 분위기
3. `sink.png` / `toilet.png` — 화장실 완성
4. `logo.png` / `lobby-bg.png` — 로비 완성도

만들어서 주시면 (채팅 첨부 or 폴더에 넣고 경로 알려주기) 제가 크기 맞춰서 넣고, 필요하면 스프라이트시트로 합치고, 코드 연결하겠습니다.
